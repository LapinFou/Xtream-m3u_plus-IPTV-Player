requests>=2.31.0,<3
lxml>=4.9.2,<7
python-dateutil>=2.8.2,<3
PyQt5>=5.15.9,<5.16
# Optional — only needed when "Use embedded player" is enabled in Settings. The
# app launches and works without it; it only fails when the user actually toggles
# the embedded player on without having it (or libvlc) installed.
python-vlc>=3.0.20000
